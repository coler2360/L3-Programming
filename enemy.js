     class Enemy {
         constructor(src, x, y, w, h, xSpeed, ySpeed) {
             this.src = src;
             this.x = x;
             this.y = y;
             this.w = w;
             this.h = h;
             this.ySpeed = ySpeed;
             this.xSpeed = xSpeed;
         }
